import React from "react";
import "./presentationBody.scss";

const PresentationBody = ({ children }) => {
  return <div className="presentationBody">{children}</div>;
};

export default PresentationBody;
